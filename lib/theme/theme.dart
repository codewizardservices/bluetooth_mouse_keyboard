import 'dart:ui';

/// Theme palette used across your keyboard/touchpad UI.
class AppThemeColors {
  AppThemeColors._();

  // Dark glass background
  static const Color bgTop = Color(0xFF14171A);
  static const Color bgMid = Color(0xFF0F1113);
  static const Color bgBottom = Color(0xFF0A0B0D);

  // Surfaces
  static const Color surface = Color(0xFF17181D);
  static const Color card = Color(0xFF1A1C21);

  // Strokes
  static const Color stroke = Color(0xFF2B2F33);

  // Accents
  static const Color accentCyan = Color(0xFF33D3FF);
  static const Color accentOrange = Color(0xFFFF5A1F);

  // Text
  static const Color textSecondary = Color(0xFF9DA0A8);
}

